## Pre-requisites
Node.js: Ensure you have Node.js installed on your system. 
You can download it from nodejs.org.

## Installation
1. Clone this repository to your local machine:

git clone https://github.com/ybaha86/fit-hub-api.git

2. Navigate to the project directory:

cd your-cypress-project

3. Install project dependencies:
npm install
4. Running Tests

-- npx cypress open
-- This command will open the Cypress Test Runner, a graphical interface for managing and running tests.
-- Click on a test file in the Cypress Test Runner to execute the tests.

